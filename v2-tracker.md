# v2 Implementation Tracker

## Current Wave: W0 (Hardening — no protocol changes)
## Status: In progress (5/6 tasks complete)
## Last Session: 2026-06-10
## Last Commit: n/a (changes in working tree, not yet committed)

## W0 Task List (from v2 design document §W0)
- [x] ETag/If-Modified-Since on Registry /list
- [x] SQL-level LIMIT/OFFSET on /list (replace JS array slice)
- [x] Rate limiting on all three services
- [x] Resolver probe_liveness: disabled by default, add endpoint allowlist
- [x] Resolver refresh jitter + exponential backoff
- [ ] /list tag filter: add index (replace LIKE scan)

## Current Task: /list tag filter — add index (replace LIKE scan)
## Blocked On: nothing
## Next Step: Replace the `tags LIKE '%"<tag>"%'` scan with an indexed lookup (e.g. a normalized tag table or expression index) in registry/server.js handleList tag path (v2 design §2.2.7; last W0 item)

## Session Notes
- 2026-06-10: Tracker created. Wave 0 scope defined from docs/dillweed-v2-design-2026-06-10.md.
- 2026-06-10: ETag/If-Modified-Since on Registry /list — DONE. Added a catalog-version counter (bumped on register/revoke/promote, seeded from registration_log at startup), strong `ETag` ("cat-<hex>") + `Last-Modified` on /list, and 304 handling for `If-None-Match`/`If-Modified-Since`. Non-breaking; no other endpoint changed (`send()` gained optional extraHeaders). Added 6 conditional-read tests to registry/test.sh; full suite 85/85 passing against an isolated instance on port 9485 (production on 9475 untouched). Committed 74cad67 on v2/w0-hardening (not pushed).
- 2026-06-10: SQL-level LIMIT/OFFSET on /list — DONE. Pushed pagination into SQL: listAll/listByTier/listByTag now carry `LIMIT ? OFFSET ?` (deterministic `ORDER BY name, version`); `total` comes from a separate COUNT (countActive / new countByTierOne / new countByTag) so it stays accurate independent of page size. handleList no longer materializes the full result set and slices in JS (closes F-10 truncation / S3 full-scan, v2 design §2.2.7). Response shape unchanged. NOTE: tag-filter indexing remains a separate W0 task (the LIKE scan is untouched here). Added 6 pagination tests to registry/test.sh; full suite 91/91 passing on the isolated 9485 instance. Committed 25670a4 on v2/w0-hardening (not pushed).
- 2026-06-10: Rate limiting on all three services — DONE. Added a per-IP fixed-window limiter (separate read vs write/expensive budgets, default 300/100 per 60s, env-overridable, /health + OPTIONS exempt) to Registry, Resolver, and Anthill. Returns 429 + Retry-After (§4.2.2) and exposes `rate_limit` in each /health. Write class: Registry=POST or /verify; Resolver=POST /batch only (plain /resolve is a cheap read); Anthill=POST /signal. Scope = rate limiting only (per the decision); fail-closed binding/CORS and the probe deny-list are NOT in this task. Cost-weighted per-identity quotas are W1. Added 4 rate-limit tests to each service's test.sh (self-calibrating bursts from /health). Verified on isolated stack (registry 9485, resolver 9484→9485, anthill 9486): Registry 95/95; Anthill 62/62 (token mode); Resolver 64 pass / 5 fail where the 5 are pre-existing seed-fixture gaps (multi-version + unsigned records absent from the 7-record dev seed) — proven environmental by an identical failure set with the limiter disabled (DILLCLAW_RL_DISABLED=1). Production (9474/9475/9476) untouched. Committed a1a95d1 on v2/w0-hardening (not pushed).
- 2026-06-10: Resolver refresh jitter + exponential backoff — DONE. Replaced the fixed `setInterval(refreshList, 60s)` with a self-scheduling setTimeout loop. Added a pure `refreshDelay(base, failures, cap, jitter, rnd)` helper: exponential backoff (doubles per consecutive failure, exponent clamped, capped at DILLCLAW_REGISTRY_BACKOFF_MAX_MS default 15min) with ±jitter (DILLCLAW_REGISTRY_REFRESH_JITTER default 0.2) — de-syncs the fleet (S1) and stops hammering a down registry (S7). refreshList() now returns success/failure to drive the failure counter; success resets to base. /health.registry.refresh exposes base_interval_ms, jitter, backoff_max_ms, consecutive_failures, next_refresh_ms. NOTE: in-flight cache-miss coalescing (also mentioned in §2.3 step 5) is NOT in this task — separate concern, candidate follow-up. Tests: 12 refreshDelay assertions in resolver/unit-tests.js (now 77/77, no server); 3 integration assertions in resolver/test.sh (config exposed, no backoff while healthy, jittered next interval); full resolver suite 69 pass / 5 fail (same environmental seed gaps). End-to-end verified against a dead registry (base 1s, jitter 0): consecutive_failures climbed 1→2→3 and next_refresh_ms doubled 2000→4000→8000 ms. Production untouched. 3 files in working tree (resolver/server.js, resolver/test.sh, resolver/unit-tests.js) — not yet committed.
- 2026-06-10: Resolver probe_liveness SSRF hardening (F-8) — DONE. probe_liveness is now OFF by default (DILLCLAW_PROBE_LIVENESS_ENABLED=1 to opt in) AND requires the caller's per-request flag. Added a self-contained `isInternalIp()` deny-list (loopback, 169.254/16 metadata, RFC-1918, CGNAT, 0/8, multicast, reserved/broadcast + IPv6 ::1/::/fe80::/10/fc00::/7/ff00::/8 and ::ffff: mapped; unparseable = unsafe/fail-closed). probeEndpoint() now resolves the host, refuses if ANY resolved address is internal, and HEAD-probes the validated IP host-pinned (Host header + SNI) to defeat DNS-rebinding. /health exposes `probe_liveness.enabled`. Scope = identity-independent hardening only; the authenticated-caller requirement and per-caller probe quota are W1 (Area 1). Tests: 35 classifier assertions added to resolver/unit-tests.js (now 65/65, no server needed); 2 integration assertions in resolver/test.sh (default-off + resolve-still-works); full resolver suite 66 pass / 5 fail (same environmental seed gaps). End-to-end verified: with probing enabled, a resolve of a capability whose endpoint is 169.254.169.254 logged `[probe] refused (resolves to internal range)` and made no outbound request. Production untouched. 3 files in working tree (resolver/server.js, resolver/test.sh, resolver/unit-tests.js) — not yet committed.
