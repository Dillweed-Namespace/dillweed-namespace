# Dillweed Namespace v1.0.0

**Released:** 2026-05-17

This is the v1 baseline release of the Dillweed Namespace. It bundles three independently-versioned components that together implement the v1 specification, each individually audit-cycled by external review and validated on the reference deployment.

## Components

| Component | Version | Tarball | SHA256 |
|---|---|---|---|
| Registry | 0.2.8 | `dillweed-registry-v0.2.8.tar.gz` | `f0e329f51ab5eb1704d496084dd02525a02ef3d754618f26b08c3a9a69d2361a` |
| Resolver | 0.1.8 | `dillweed-resolver-v0.1.8.tar.gz` | `2e3376a50c8485607c614fccbac44d3ffd9f222550ad1e5f97b6c7e45c814f0a` |
| Anthill  | 0.1.6 | `dillweed-anthill-v0.1.6.tar.gz`  | `3bda022d2213240cfbc4355e6c07e85b8f8b997a7ae398ad626f5cb58f574f36` |

The tarballs are attached to this release as downloadable assets.

## What's in v1

**Specification conformance** — all three components implement the v1 specifications published at [dillweed.com](https://dillweed.com). Specifications were finalized across May 7–16, 2026, with eight specification documents published at dillweed.com: Namespace Standard, Registry Specification, DillClaw Resolver Specification, Anthill Specification, Standards Overview, Governance Framework, Continuity Protocol, and DNSO Operations Charter.

**Trust root** — the canonical DNSO public key is published at `https://dillweed.com/dnso_public.pem` with SHA256 `909891e92cfa7362ee88fd75e85f379a317680a2c987fc4d88ecae150deb6f33`. The Resolver v0.1.8 installer fetches this automatically; no manual key placement is required.

**External audit** — each component went through multiple rounds of external review before reaching v1 baseline:
- Registry: 5 review rounds, converged at v0.2.7 ship-equivalence
- Resolver: 3 review rounds + final-pass addendum, converged at v0.1.7
- Anthill: 3 review rounds, converged at v0.1.4

**Install testing** — three install modes exercised on `dill-p-001` (macOS 26.5, Node.js v22.22.2): in-place upgrade, uninstall, and clean install from pristine tarballs. Testing surfaced the issues that drove the v1 patch round.

**v1 patch round** — three follow-up patches address findings from install testing:

| Patch | Closes | Description |
|---|---|---|
| Resolver v0.1.7 → v0.1.8 | INST-004 (HIGH) | Installer now fetches the canonical DNSO public key from `dillweed.com` at install time instead of shipping a (wrong) bundled key |
| Registry v0.2.7 → v0.2.8 | INST-001 (LOW), INST-005 (MEDIUM) | `install.sh` aborts cleanly if run from inside the install destination; `test.sh` now supports `REGISTRY_ADMIN_TOKEN` env var |
| Anthill v0.1.4 → v0.1.5 | INST-006 (LOW) | `test.sh` now appends a per-run unique suffix to `originating_node` values so tests don't collide with preserved `node_sequences` high-water-marks after in-place upgrades |

**Post-v1 patch:** Anthill v0.1.5 → v0.1.6 (commits 7a553f1, da6ab0c) — fixes AS-006
body-size test (tempfile pattern), fixes req.destroy preventing 413 delivery, adds Keychain
token fallback to test.sh. Closes INST-013.

The only HIGH-severity install finding (INST-004) is closed.

## Validation summary

```
Component   Test result after patch round
─────────────────────────────────────────
Registry    79/79 pass
Resolver    /health reports configured trust root; signature verification works end-to-end
Anthill     58/58 pass
```

Trust chain consistency confirmed across all three locations:
- Registry keystore: `909891e92cfa7362...`
- Resolver keystore: `909891e92cfa7362...`
- Published `dillweed.com/dnso_public.pem`: `909891e92cfa7362...`

End-to-end: `/verify/<capability-name>` returns `signature_valid: true` for seeded records.

## Verifying integrity after download

After downloading the tarballs from this release:

```bash
sha256sum dillweed-registry-v0.2.8.tar.gz \
          dillweed-resolver-v0.1.8.tar.gz \
          dillweed-anthill-v0.1.6.tar.gz
```

Expected output:

```
f0e329f51ab5eb1704d496084dd02525a02ef3d754618f26b08c3a9a69d2361a  dillweed-registry-v0.2.8.tar.gz
2e3376a50c8485607c614fccbac44d3ffd9f222550ad1e5f97b6c7e45c814f0a  dillweed-resolver-v0.1.8.tar.gz
3bda022d2213240cfbc4355e6c07e85b8f8b997a7ae398ad626f5cb58f574f36  dillweed-anthill-v0.1.6.tar.gz
```

## Install order

1. Registry (port 9475)
2. Resolver (port 9474) — point it at the Registry
3. Anthill (port 9476) — point instrumented Resolvers at it

Detailed install instructions are in the repository [README](../README.md).

## Known issues

Carried into v1.0.0 (none HIGH or MEDIUM):

- **INST-008 (LOW)** — partial closure; the Resolver tarball filename was corrected from `dillclaw-resolver-v0.1.8.tar.gz` to `dillweed-resolver-v0.1.8.tar.gz` at v1.0.0 publication. The tarball still extracts to a `dillclaw-resolver/` directory (the component-internal name) — cosmetic inconsistency, queued for cleanup in a future release
- **INST-011 (info)** — admin tokens in launchd plist files are stored plaintext (acceptable for reference deployment)
- **INST-012 (info)** — Registry tarball ships unused `resolver-patch.js` artifact (cosmetic)

Full ledger: [`PROJECT_LEDGER.md`](../PROJECT_LEDGER.md).

## Deferred to v2

- **AUDIT-AS-001** — coordinated node-signature cryptographic verification (the entry point for Anthill v2 work)

## License

Apache License, Version 2.0.
